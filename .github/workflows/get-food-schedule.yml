name: Get Food Schedule
on:
  schedule:
  - cron: "0 1 * * 1-5"
jobs:
  refresh-feed:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch API Data 📦
        uses: JamesIves/fetch-api-data-action@v2
        with:
          endpoint: https://www.skolmaten.se/api/3/menu/?client=${{ secret.SKOLMATEN_CLIENT_ID }}&school=6527553372160000
          configuration: '{ "method": "GET" }'
          save-location: food-schedule/data.json
